<Window
    x:Class="VRASDesktopApp.ServerSettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Server Settings"
    Width="500" Height="450"
    WindowStartupLocation="CenterOwner"
    Background="{StaticResource Gray50}"
    FontFamily="Roboto"
    Style="{StaticResource ChromeWindow}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="SERVER SETTINGS"
                   Style="{StaticResource Heading5}"
                   Margin="0,0,0,16" />

        <TextBlock Text="API Base URL" Style="{StaticResource FormHeading}" Grid.Row="1" />
        <TextBox Name="txtApiBaseUrl" Style="{StaticResource MaterialTextBox}" Grid.Row="2" Margin="0,4,0,12" />

        <TextBlock Text="API Key" Style="{StaticResource FormHeading}" Grid.Row="3" />
        <TextBox Name="txtApiKey" Style="{StaticResource MaterialTextBox}" Grid.Row="4" Margin="0,4,0,12" />

        <TextBlock Text="Firm Name" Style="{StaticResource FormHeading}" Grid.Row="5" />
        <TextBox Name="txtFirmName" Style="{StaticResource MaterialTextBox}" Grid.Row="6" Margin="0,4,0,12" />

        <TextBlock Text="Contact Numbers" Style="{StaticResource FormHeading}" Grid.Row="7" />
        <TextBox Name="txtContactNos" Style="{StaticResource MaterialTextBox}" Grid.Row="8" Margin="0,4,0,12" />

        <StackPanel Grid.Row="9" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Name="btnCancel" Click="btnCancel_Click"
                    Content="CANCEL"
                    Background="{StaticResource Gray300}"
                    BorderBrush="{StaticResource Gray300}"
                    Foreground="{StaticResource Gray800}"
                    Margin="0,0,8,0"
                    Height="40" Padding="24,0"
                    FontWeight="SemiBold"
                    Cursor="Hand" />
            <Button Name="btnSave" Click="btnSave_Click"
                    Content="SAVE"
                    Background="{StaticResource Primary500}"
                    Foreground="White"
                    BorderThickness="0"
                    Height="40" Padding="24,0"
                    FontWeight="SemiBold"
                    Cursor="Hand" />
        </StackPanel>
    </Grid>
</Window>
