# .NET
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs

# VS Code
.vscode/

# Build results
[Dd]ebug/
[Rr]elease/
x64/

# NuGet
packages/
*.nupkg

# Local environment
db/.env
db/.env.local
.env

# Node
node_modules/

# Test/scratch files with credentials
test.js

# Ops runbook containing the live Cloudflare API token — must NOT be pushed.
AUTO_SSL_RENEWAL.md

# Python bytecode cache (tools/)
__pycache__/
*.pyc

# Per-build transient — tools/build_wpf_*.py write the current agency's branding
# here and delete it on exit; never commit a tenant's branding into the repo.
VKdesktopapp/Resources/branding.json

# Claude Code
.claude/

# Android build artifacts
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/**/*.apk
android/**/*.aab

# WPF dotnet publish output (raw runtime — use installer-output/ for distribution)
VKdesktopapp/publish-output/
VKdesktopapp/publish-fresh/

installer-output/
VKdesktopapp/publish/
VKdesktopapp/publish-fresh/
VKApiServer/bin/publish/
VKmobileapi/bin/publish/
android/keystore/

# Windows SDK BuildTools fetched for MSIX packaging (large, machine-local)
tools/_sdk/
