<Window
    x:Class="CRMRSDesktopApp.MessagesWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Statements"
    Width="780" Height="640"
    WindowStartupLocation="CenterOwner"
    Background="{StaticResource Gray50}"
    FontFamily="{StaticResource FontBody}"
    Style="{StaticResource ChromeWindow}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="{StaticResource Gray900}"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="40" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="STATEMENTS FROM FINANCE COMPANIES" VerticalAlignment="Center" Margin="14,0,0,0"
                       FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource Orange500}"
                       IsHitTestVisible="False" />
            <Button Grid.Column="1" Name="btnClose" Click="btnClose_Click"
                    Background="Transparent" BorderThickness="0" Content="&#x2715;"
                    FontSize="14" Foreground="{StaticResource Gray400}" Cursor="Hand"
                    WindowChrome.IsHitTestVisibleInChrome="True" />
        </Grid>

        <Grid Grid.Row="1" Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <TextBlock Text="Messages sent to you by the finance companies you are integrated with."
                           Foreground="#FF6B7280" VerticalAlignment="Center" FontSize="13" />
                <Button Name="btnRefresh" Content="&#x21BB;  Refresh" Click="btnRefresh_Click"
                        Margin="16,0,0,0" Padding="12,5" Cursor="Hand" />
            </StackPanel>

            <Border Grid.Row="1" Background="White" CornerRadius="12"
                    BorderBrush="#FFE5E7EB" BorderThickness="1">
                <Grid>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8">
                        <ItemsControl Name="listMessages">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#FFF9FAFB" CornerRadius="10"
                                            BorderBrush="#FFE5E7EB" BorderThickness="1"
                                            Margin="6" Padding="14">
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="{Binding FromFinance}" FontWeight="Bold" FontSize="14.5" />
                                                <TextBlock Text="{Binding CreatedAt}" Foreground="#FF9CA3AF"
                                                           FontSize="12" Margin="12,3,0,0" />
                                            </StackPanel>
                                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap"
                                                       Margin="0,7,0,0" FontSize="13.5" Foreground="#FF111827" />
                                            <TextBlock Text="{Binding FromEmail}" Foreground="#FF9CA3AF"
                                                       FontSize="11.5" Margin="0,7,0,0" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    <TextBlock Name="emptyLabel" Text="No statements yet."
                               Foreground="#FF9CA3AF" HorizontalAlignment="Center"
                               VerticalAlignment="Center" Visibility="Collapsed" FontSize="14" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
