<Window
    x:Class="VRASDesktopApp.DirectData.AddCredentialDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Add Webhook Credential"
    Width="380" Height="220"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterOwner"
    Background="White"
    FontFamily="{StaticResource FontBody}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Username" FontSize="12" Foreground="#FF555555" Margin="0,0,0,4" />
            <TextBox Name="txtUsername" Height="32" Padding="8,4" FontSize="13"
                     BorderBrush="#FFCCCCCC" BorderThickness="1" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Password" FontSize="12" Foreground="#FF555555" Margin="0,0,0,4" />
            <TextBox Name="txtPassword" Height="32" Padding="8,4" FontSize="13"
                     BorderBrush="#FFCCCCCC" BorderThickness="1" />
        </StackPanel>

        <TextBlock Grid.Row="4" Name="lblError" Foreground="#FFE53935" FontSize="12"
                   Visibility="Collapsed" />

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Height="32" Margin="0,0,8,0" Cursor="Hand"
                    Click="btnCancel_Click" />
            <Button Content="Create" Width="80" Height="32" Cursor="Hand"
                    Background="#FFFF9800" Foreground="White" BorderThickness="0"
                    Click="btnCreate_Click" />
        </StackPanel>
    </Grid>
</Window>
